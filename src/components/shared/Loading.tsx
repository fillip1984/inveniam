import { FaCircleNotch } from "react-icons/fa";

const Loading = () => {
  return (
    <FaCircleNotch className="mx-auto mt-24 animate-spin text-8xl text-primary" />
  );
};

export default Loading;
